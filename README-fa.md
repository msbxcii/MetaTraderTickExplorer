# متاتریدر تیک اکسپلورر

برنامه "متاتریدر تیک اکسپلورر" یک اپلیکیشن ویندوزی است که داده های تیک را از طریق ترمینال لوکال متاتریدر در سیستم استخراج میکند و سپس از آن برای ساختن کندل های ثانیه (و بالاتر) استفاده میکند.این داده ها مستقیما در دیسک ذخیره میشوند و به کاربر امکان استفاده از چارت با دقت ثانیه در لایو بازار و همچنین هر تاریخی از گذشته را فراهم میکند. داده های بدست آمده از طریق پروژه متن باز رسمی [تریدینگ ویو Lightweight chart ](https://www.tradingview.com/lightweight-charts/)اجرا میشوند. 

### از جمله قابلیت های این نرم افزار:

1) مشاهده چارت در حالت **Replay Mode** با سرعت واقعی لایو بازار (1 تیک در ثانیه ) که به کاربر امکان شبیه سازی واقع گرایانه برای تمرین را میدهد.
2) امکان استفاده از حالت **مولتی چارت** برای مشاهده نمودار در چندین تایم فریم مختلف زمانی که از آن میتوان هم در حالت Live و هم در حالت Replay بهره برد.
3) اجرای کاملا **آفلاین** برنامه براساس دیتابیس ذخیره شده برای تمرین بر روی داده های گذشته
4) (در آینده) اجرای مستقیم معاملات در داخل برنامه

### 🔒 امنیت و حریم خصوصی

- **بدون اتصال به سرور خارجی:** برنامه فقط با **ترمینال محلی MetaTrader** (از طریق API رسمی MetaTrader برای Python) در ارتباط است و داده‌ای به هیچ سرویس شخص ثالثی ارسال نمی‌شود.
- **بدون دسترسی به اطلاعات حساب:** نیازی به نام کاربری، رمز عبور یا هیچ اطلاعات ورود دیگری ندارد؛ این اطلاعات همیشه فقط در اختیار خود ترمینال MetaTrader می‌ماند.
- **داده‌ها محلی هستند:** تمام داده‌های تیک/کندل و تنظیمات، صرفاً روی سیستم خود کاربر ذخیره می‌شوند (جزئیات محل ذخیره‌سازی در بخش زیر).

### 💾 محل ذخیره‌سازی داده‌ها و لاگ‌ها

محل ذخیره پوشه‌های `output` (دیتابیس کندل‌ها/تیک‌ها، تنظیمات و ...) و `logs` بسته به روش اجرای برنامه فرق می‌کند:

- **اجرا از طریق سورس (`run_chart.cmd` / دولوپرها):**
  همه‌چیز داخل همین ریپازیتوری ذخیره می‌شود:
  ```text
  <repository-folder>\output\
  <repository-folder>\logs\
  ```

- **اجرا از طریق فایل `.exe`:**
  برای اینکه فایل EXE کاملاً پورتابل باشد (روی دسکتاپ، فلش یا هر مسیر دیگری)، داده‌ها و لاگ‌ها کنار خود EXE ذخیره نمی‌شوند و به جای آن در پوشه‌ی اختصاصی برنامه در AppData کاربر ویندوز نوشته می‌شوند:
  ```text
  %LOCALAPPDATA%\MT-TickExplorer\output\
  %LOCALAPPDATA%\MT-TickExplorer\logs\
  ```
  (این مسیر معمولاً معادل `C:\Users\<username>\AppData\Local\MT-TickExplorer\` است.)

---

## پیش‌نیازهای عمومی

- **ویندوز ۱۰ یا ۱۱** 
- **متاتریدر ۵** نصب‌شده، با شرایط زیر:
  - **فعال بودن Python Integration** — در ترمینال از مسیر
    `Tools › Options › Community` آن را فعال کنید.
  - **لاگین بودن در یک حساب** (دمو یا واقعی) — بدون این کار، بروکر هیچ داده‌ای
    در اختیار برنامه قرار نمی‌دهد.

## شروع کار

### کاربران عادی

آخرین فایل `.exe` را از بخش **Releases** پروژه دانلود و اجرا کنید — نیازی
به Python یا هیچ تنظیم دیگری نیست. در اولین اجرا، مراحل زیر طی می‌شود:
### دولوپرها

```bash
git clone <repository-url>
cd <repository-folder>
pip install -r requirements.txt
run_chart.cmd
```

## ساخت نسخه Release با EXE

پروژه برای ساخت نسخه ویندوزی، تنظیمات استاندارد PyInstaller و فایل
`build.cmd` را دارد :

 در محیط ویندوز که Python و PyInstaller در آن نصب است، اجرا کنید:

```cmd
build.cmd
```

فایل نهایی در مسیر زیر ساخته می‌شود:

```text
dist\MetaTrader Tick Explorer.exe
```

